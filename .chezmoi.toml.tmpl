# Usage:
# chezmoi git pull -- --autostash --rebase && chezmoi diff  
# chezmoi apply -v

{{- $email := promptStringOnce . "email" "Email address" -}}
{{- $shell := promptStringOnce . "shell" "Your shell [zsh|fish|bash]" -}}
{{- $eneng := promptStringOnce . "eneng" "Encryption engine [age]"  }}

[data]
  email = {{ $email | quote }}
  shell = {{ $shell | quote }}
  eneng = {{ $eneng | quote }}

[data.features]
  dummy = false

[data.install]
  age = {{ eq $eneng "age" }}
  gopass = true
  gnutools = true     # Provide GNU tools by symlinking into ~/bin
  kitty = false


encryption = {{ $eneng | quote }}

[age]
    identity = "~/.ssh/id_ed25519"
    symmetric = true

[git]
    autoCommit = true
    #autoPush = true
    commitMessageTemplate = {{ `"{{ promptString \"Commit message\" }}"` }}

{{ $el :=  "{{"  }}
{{ $er :=  "}}"  }}
[merge]
    command = "nvim"
    args = ["-d", "{{ $el }} .Destination {{ $er }}", "{{ $el }} .Source {{ $er }}"]

